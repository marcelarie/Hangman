let test = 'witch'
let selectedWord = [test];
let emptyString = [];

/*if letter selected  matches  selectedWord  content */
/*display identified letter = visible*/
/*divide word into letters */
/* put arrays of letters into another variable */
/*for each of each letter in the variable
/*appendchild to the parent element*/
/*add a class to the parent element*/
/*append child with styles*/

/*ClassList*/

/* Select an array and split it into elements */

let splitted = selectedWord.split(" ");
for (let i = 0; i < splitted.length; i++) {
    emptyString.push(splitted[i])
    console.log(emptyString)
}

